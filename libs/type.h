#ifndef TYPE_H
#define TYPE_H

#define EOL '\n'

bool isidentnonalnum(char c);
bool isidentstart(char c);
bool isident(char c);
bool isaddoperator(char c);
bool ismuloperator(char c);
bool ismodoperator(char c);
bool islogicoperator(char c);
bool isoperator(char c);

#endif
